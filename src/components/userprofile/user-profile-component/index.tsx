import React, { useState, useEffect } from "react";
import {
  Container,
  Form,
  ASText,
  TextField,
  SelectField,
  Text,
} from "app-studio-widgets";

const UserProfileComponentComponent: React.FC<any> = ({
  navigation,
  route,
}) => {
  return <></>;
};

export default UserProfileComponentComponent;
